WEBLINC.facebook = (function () {
    'use strict';

    var init = function () {};

    WEBLINC.modules.onDomReady(init);

    // TODO Determine if this module needs to run
    // on DOM update.
    // WEBLINC.modules.onDomUpdate(init);

    return {
        init: init
    };
}());
